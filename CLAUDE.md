# Weebly-to-Firebase

Personal CLI tool. Solo-owned, master-first commit flow (see
`~/Work Files/working-style.md`).

## Changelog: opt out

This repo does **not** use the changelog / Track logging protocol from the
global CLAUDE.md. Skip both `changelog.md` and the Track `task_log` API for
work in this directory. `git log` is the source of truth for what changed.
